// import { expect, test } from '@playwright/test';
// import { adminAuthFile } from '../auth.setup';

// test.describe(() => {
//   test.use({ storageState: adminAuthFile });

//   test('can see admin dashboard as an admin', async ({ page }) => {
//     await page.goto('http://localhost:3000/');
//     await page.getByRole('link', { name: 'Admin Admin' }).click();
//     await expect(await expect(page).toHaveURL('/admin'));
//   });
// });
