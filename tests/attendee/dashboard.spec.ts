// import { expect, test } from '@playwright/test';
// import { attendeeAuthFile } from '../auth.setup';

// test.describe(() => {
//   test.use({ storageState: attendeeAuthFile });

//   test('can NOT see admin dashboard link as an attendee', async ({ page }) => {
//     await page.goto('http://localhost:3000/');
//     await expect(page.getByRole('link', { name: 'Admin Admin' })).toBeHidden();
//   });
// });
